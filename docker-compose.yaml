version: '3.9'

services:
  websocket-server:
    hostname: serverhost
    build: ./websocket-server
    ports:
      - 8080:8080
  websocket-client:
    hostname: clienthost
    build: ./websocket-client
    ports:
      - 3000:3000